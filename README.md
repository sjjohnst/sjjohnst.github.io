# Sam Johnstons personal website

This repository holds the source code for my personal static site.

The site is built using Jekyll, a static site generator, and is hosted on GitHub Pages.
To run the site locally, you can use the following command:

```
bundle exec jekyll serve
```